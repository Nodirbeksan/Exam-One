#include <iostream>

using namespace std;

int main()
{
    int a;

    cout << " a tomonni kiriting : ";
    cin >> a;

    int V = a * a * a;
    int S = 6 * a * a;

    cout << " kubning hajmi : " << V << endl;
    cout << " kubning yuzi : " << S << endl;

    return 0;
}
